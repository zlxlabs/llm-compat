# @generated by docker-package skill
# @skill-version 3
# Stored in docker/ locally. pull_and_deploy.sh copies to project root as docker-compose.yml.
# .env must be in the same directory as docker-compose.yml on the server.
services:
  llm-compat-collector:
    image: ghcr.io/zj1123581321/llm-compat-collector:latest
    container_name: llm-compat-collector
    env_file:
      - .env
    ports:
      - "127.0.0.1:8234:8000"
    volumes:
      - ./data:/data
    restart: unless-stopped
    networks:
      - llm-net
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"

networks:
  llm-net:
    name: llm-net
    external: true
